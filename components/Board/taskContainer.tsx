import React from "react"
import {TaskContainer as Container} from "./style"

interface Props {
  children?: any
}

const TaskContainer: React.FC = ({children}) => {
  return (
    <Container>
    </Container>
  )
}

export default TaskContainer
